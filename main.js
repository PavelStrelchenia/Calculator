"use strict";

// Addition

function sum(a, b) {
    return a + b;
}

// Subtraction

function sub(a, b) {
    return a - b;
}

// Multiplication

function multi(a, b) {
    return a * b;
}

// Division

function division(a, b) {
    return a / b;
}

//
